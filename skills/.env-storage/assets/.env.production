# .env.production — production environment (gitignored; never commit).
# Populated into the project by the .env-storage skill (Load), which copies
# this file's master copy here. The model never sees secret values. Contains
# the PROD TRIGGER_SECRET_KEY only — never the dev key. Also read by the Modal
# bridge (Secret.from_dotenv(__file__, filename=".env.production")).
#
# Trigger.dev production secret
TRIGGER_SECRET_KEY=tr_prod_REPLACE-ME...
# TRIGGER_PROJECT_ID does NOT go here — it lives in trigger.config.ts.