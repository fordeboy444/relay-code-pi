# .env.production — production environment (gitignored; never commit).
# Populated by the relay-system-setup skill (Part A) via the .env-storage skill
# (Load), which copies your master .env.production here — the same skill that
# populates .env. The model never sees secret values. Contains the PROD
# TRIGGER_SECRET_KEY only. This file is also read by the Modal bridge
# (Secret.from_dotenv(__file__, filename=".env.production")) — never put the
# dev key here.
#
# Expected line once .env-storage (Load) has run:
#   TRIGGER_SECRET_KEY=<prod tr_prod_… key>
# TRIGGER_PROJECT_ID does NOT go here — it lives in trigger.config.ts.