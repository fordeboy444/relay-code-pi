# .env — development environment (gitignored; never commit).
# Populated by the relay-system-setup skill (Part A) via the .env-storage skill
# (Load), which copies your master .env here. The model never sees secret values.
#
# Expected lines once .env-storage (Load) has run:
#   TRIGGER_SECRET_KEY=<dev tr_dev_… key>
#   MODAL_TOKEN_ID=<ak-… token id>
#   MODAL_TOKEN_SECRET=<as-… token secret>
#   AIRTABLE_TOKEN=<…>            (only if the first automation uses Airtable)
# TRIGGER_PROJECT_ID does NOT go here — it lives in trigger.config.ts.