# Comments

Remove a like from a comment by its **Highlevel** comment ID (the `_id` returned by the list-comments endpoint — not the native platform ID).

## Pages in this folder

| Page | Local file | Summary |
| --- | --- | --- |
| Unlike a comment | [unlike-a-comment.md](unlike-a-comment.md) | Remove a like from a comment by its **Highlevel** comment ID (the `_id` returned by the list-comments endpoint — not the native platform ID). |
| List comments for a post or thread | [list-comments-for-a-post-or-thread.md](list-comments-for-a-post-or-thread.md) | Paginated list of comments scoped to a post (`parentId` = postId) or a comment thread (`parentId` = commentId). Use `skip`/`limit` for pagination, `sortBy` for ordering, `originIds` to filter by connected account, and `search` for keyword search. |
| Like a comment | [like-a-comment.md](like-a-comment.md) | Like a comment by its **Highlevel** comment ID (the `_id` returned by the list-comments endpoint — not the native platform ID). |
| Create a comment or reply | [create-a-comment-or-reply.md](create-a-comment-or-reply.md) | Create a top-level comment on a post (`isParentThread: true`, `parentId` = postId) or a reply to an existing comment (`isParentThread: false`, `parentId` = commentId). Per-platform content max length: Facebook 8000, Instagram 2200, Linkedin 3000, Community 8000, Tiktok 150, Bluesky 300, Youtube 10000, Threads 500. |
| Comments | [comments.md](comments.md) | Comments |