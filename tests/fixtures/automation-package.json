{
  "name": "REPLACE-ME-via-system-setup",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "relay-code automation (Modal.com + Trigger.dev). Scaffolded by /system-setup.",
  "scripts": {
    "trigger:dev": "trigger dev",
    "trigger:deploy": "trigger deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@trigger.dev/sdk": "^4.5.12",
    "@trigger.dev/build": "^4.5.12"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "@types/node": "^22.0.0"
  }
}